apiVersion: v1
kind: Service

metadata:
  name: smartdeploy-service

spec:
  type: NodePort

  selector:
    app: smartdeploy

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080